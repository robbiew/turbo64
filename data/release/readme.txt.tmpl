=============================================================================
TURBO/64 BBS (T/64) v__VERSION__
A Commodore 64 BBS for the Oscar64 compiler
=============================================================================

Note: This project is under active development and is not yet
feature-complete.

WHAT THIS IS
------------

TURBO/64 BBS is a Commodore 64 BBS written in C for the Oscar64 compiler.
It targets native .prg output for real hardware (including C64 Ultimate)
and VICE emulation.

Current status: v__VERSION__ -- login/registration, terminal translation
(PETSCII, ANSI/CP437, ASCII), bulletin boards, door programs (external
Oscar64 plug-ins, with an author dev kit), Punter and Zmodem file
transfers, and the Configure editor are working. As of v__VERSION__, T/64
ships TWO builds -- see WHICH BUILD DO I WANT below.

Not working: private mail, SysOp chat, polls/voting, and more remain
stubbed.

WHICH BUILD DO I WANT
----------------------

  Your hardware                          | Build            | Media
  ----------------------------------------+------------------+-------------
  C64 Ultimate, Software IEC             | *-SIEC.prg       | a folder on
                                          |                  | USB/SD; no
                                          |                  | disk image
  sd2iec/uIEC/1571/1581/emulated 1581    | plain .prg       | the .d81

If you're not sure: if you access your Ultimate's storage as a mounted
USB/SD folder tree (not a .d81 you attach to a virtual drive), you want
the -SIEC build. Everyone else wants the plain build and the .d81.

The SIEC build has two hard requirements: an REU enabled in the
Ultimate's menu (without one, every database operation fails), and a
folder tree produced by tools/migrate-d81.py (included in this archive)
-- see SOFTIEC QUICK START below. Known gaps: free-space displays read 0
(SoftIEC reports no block count), and each storage section is one flat
16-character namespace.

REQUIREMENTS
------------

Hardware / emulator:
- Commodore 64 with SwiftLink/ACIA cartridge at $DE00, OR C64 Ultimate
  with built-in ACIA, OR VICE x64sc with tcpser modem bridge
- REL build: two .D81 disk images -- one for the BBS, one blank for
  message boards (device 9)
- SIEC build: a C64 Ultimate with Software IEC storage and an REU enabled
- 16 MB REU required for message boards and some other features on the
  REL build (hard-required on the SIEC build). The C64 Ultimate has one
  built in; VICE can be configured with one.

Developer build host (macOS / Linux):
- oscar64 compiler -- bash tools/install-oscar64.sh
- c1541 from VICE package (disk assembly; also needed to run
  migrate-d81.py for a SIEC install)
- c64u deploy tool for C64U -- bash tools/install-c64u.sh
- tcpser for VICE modem emulation -- macOS: brew install tcpser
- python3 (SIEC installs only, to run migrate-d81.py)

QUICK START (REL build -- disk image)
--------------------------------------

1. Mount TURBO64-__VERSION__.d81 on device 8.

2. Initialize the files (first time only):
   LOAD "CONFIGURE-__VERSION__",8
   RUN
   Choose I -- INIT FILES from the main menu.
   This creates USR LOG, USR PROF, and CALLERS.
   Record 1 is always the SysOp account: handle SYSOP, password PASS,
   access level 5.  Change it!

3. Configure the BBS:
   From CONFIGURE, choose C -- CONFIG to edit settings, devices, modem
   type, and baud rate.  Changes are saved to the config SEQ file
   immediately.

4. Set up message boards disk (device 9):
   Format a blank .D81, mount it on device 9.  In CONFIGURE, choose
   M -- MSG AREAS then C -- CREATE to add boards.

5. Run the BBS:
   LOAD "BOOT-__VERSION__",8
   RUN

   Expected startup:
     TURBO/64 BBS V__VERSION__
     LOADING SETUP...
       BBS: <your bbs name>
       SYSOP: <your name>
     INIT MODEM...
       ACIA STATUS: $10
     CHECKING REU...
       REU: 16 MB
     CHECKING USR LOG...
       USR LOG: OK

   If USR LOG: NOT FOUND or USR LOG: EMPTY, return to CONFIGURE and
   run Init Files.

SOFTIEC QUICK START (C64 Ultimate only -- no disk image)
----------------------------------------------------------

This archive keeps the SIEC binaries and migrate-d81.py under the same
relative paths the project itself uses (tools/, include/bbs/,
build/c64/siec/), so the commands below work unzip-and-cd with no extra
flags.

1. Enable an REU in the Ultimate's menu, if not already on.

2. From the unzipped archive folder, build the SoftIEC tree from the
   included .d81 (needs python3 and c1541 on your PATH):
     python3 tools/migrate-d81.py TURBO64-__VERSION__.d81 siec-tree \
         --base /USB1/TURBO64 --device 11
   (--base is where the tree will live on the stick; --device is the
   SoftIEC device number, 11 by default.)

   The tree includes both BOOT-SIEC.prg and CONFIGURE-SIEC.prg, so there
   is nothing to copy by hand. Every data file in it is named the way the
   Ultimate itself names files the C64 writes (usr log.seq, callers.seq,
   config.seq, ...); leave those names alone.

3. Copy the entire contents of siec-tree/ onto the USB stick or SD card
   at the --base path you gave above, so that siec-tree/SYSTEM becomes
   /USB1/TURBO64/SYSTEM, and so on. Do not mount anything on device 11.

4. Boot:
     LOAD"BOOT-SIEC",11
     RUN

   To run the SysOp editor instead:
     LOAD"CONFIGURE-SIEC",11
     RUN

   If the BBS reports section-folder or REU errors at boot, re-check the
   REU is enabled and that the tree landed at exactly the --base path you
   migrated for.

CONFIGURATION REFERENCE
-----------------------

data/config default values:

  BBS_NAME         A NEW T/64 BBS
  BBS_CITY         SOMEWHERE, CA
  SYSOP_NAME       JOE SYSOP
  NEW_USER_LEVEL   1
  MIN_CALL_TIME    5
  MAX_CALL_TIME    60
  DEV_SYSTEM       8
  DEV_MSGS         9
  DEV_FILES        8
  DEV_DOORS        8
  MODEM_INIT       ATZ
  BAUD_RATE        38400
  MODEM_TIMEOUT    255
  ALLOW_NEW_USERS  1
  ALLOW_UPLOADS    1

Device values can include a drive number and CBM DOS init string:
8, 8;0:, or 8;0:;CD:BBS.  The BBS sends the init string to the drive
at startup.

ACCESS LEVELS
-------------

  Level  Name      Description
  ------ --------- ---------------------------------------------------
  0      Deleted   Banned / deleted account
  1      New       Unvalidated new user (default for registrants)
  2      User      Standard validated user
  3      Power     Power user
  4      Co-SysOp  Co-SysOp
  5      SysOp     Full access; cannot be deleted

Each level has daily limits and capability flags edited in CONFIGURE:
  CALLS/DAY  -- max logins per day for the level
  MINS/DAY   -- max online minutes per day (enforced live)
  FLAGS      -- capability bits (A=anon, P=page, S=send mail,
                J=join polls, U=upload, T=no time limit,
                C=no call limit, Y=sysop)

WHAT IS NOT YET IMPLEMENTED
----------------------------

The following features return a placeholder message:

- Private mail
- SysOp chat / page
- Polls and votes
- Last caller display
- System info
- Graffiti wall
- Preferences (can't edit)
- Help
- Local chat
- 80-column (ANSI) message viewer
- WFC F keys (local login, log viewer, etc.)
- Spy F keys (chat, change access level)
- Adapt message area to 80 cols

Data structures and CONFIGURE admin modules for all of the above are
in place.

HOW TO CONTRIBUTE
-----------------

TURBO/64 is an active project and contributions of all kinds are welcome
-- you do not need to be a C programmer to help.

Developers: the stubbed features listed above are the priority.  Each one
has its data layer and CONFIGURE admin UI in place; what is missing is
the caller-facing session logic inside src/features/.  If you want to
pick up a feature, open an issue first to avoid duplication.  Good entry
points: src/features/mail.c, src/features/vote.c,
src/features/callers.c -- all thin stubs waiting to be wired up on the
same pattern as src/features/bulletin.c.

Artists and SysOps: the BBS ships with bare-bones gfiles.  Needed:
PETSCII art (mode 0), ANSI/CP437 art (mode 1), ASCII variants (mode 2),
and menu prompt files (p.*).  Files live in data/gfiles/ and are
assembled directly onto the disk image by make disk.

Testers: real-hardware and emulator coverage is critical.  Specifically
needed: SD2IEC (REL file edge cases), VICE regression testing, real
1541/1571/1581 drives, and terminal clients (SyncTerm, PuTTY, netcat,
real C64 terminals).

File issues on GitHub with hardware/firmware versions, a description of
what you did, and the exact error or unexpected behaviour.

GITHUB
------

https://github.com/robbiew/turbo64